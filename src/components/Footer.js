const styles = {
  footer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: "center",
    width: '100%',
    margin: '0',
  }
}

export default function Footer() {
  return <div style={styles.footer}>Footer component</div>;
}
